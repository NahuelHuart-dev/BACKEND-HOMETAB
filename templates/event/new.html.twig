{% extends 'base.html.twig' %}

{% block title %}Nou esdeveniment · HomeTab{% endblock %}

{% block body %}
<div class="ht-page-head">
    <div>
        <div class="ht-mini-tag">Esdeveniments</div>
        <h1 class="ht-display">Nou esdeveniment</h1>
    </div>
    <a href="{{ path('app_event_index') }}" class="ht-btn ht-btn--secondary"><span class="pi pi-arrow-left"></span> Llistat</a>
</div>

<div class="ht-card" style="max-width:720px;">
    {{ include('event/_form.html.twig') }}
</div>
{% endblock %}
